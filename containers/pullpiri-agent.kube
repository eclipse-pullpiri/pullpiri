[Unit]
Description=Pullpiri agent
After=network.target

[Install]
# Start by default on boot
WantedBy=multi-user.target default.target

[Kube]
Yaml=/etc/containers/systemd/pullpiri/pullpiri-agent.yaml

[Service]
Restart=always
