[Unit]
Description=Pullpiri player
After=network.target

[Install]
# Start by default on boot
WantedBy=multi-user.target default.target

[Kube]
Yaml=/etc/containers/systemd/pullpiri/pullpiri-player.yaml

[Service]
Restart=always
