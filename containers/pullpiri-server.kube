[Unit]
Description=Pullpiri server
After=network.target

[Install]
# Start by default on boot
WantedBy=multi-user.target default.target

[Kube]
Yaml=/etc/containers/systemd/pullpiri/pullpiri-server.yaml

[Service]
Restart=always
