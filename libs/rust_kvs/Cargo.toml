[package]
name = "rust_kvs"
version = "0.1.0"
edition = "2021"

[dependencies]
adler32 = "1.2.0"
tinyjson = "2.5.1"

[dev-dependencies]
tempfile = "3.20"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage_nightly)'] }
